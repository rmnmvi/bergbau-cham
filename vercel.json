{
  "buildCommand": "npm run build:css && mkdir -p dist && cp index.html dist/ && cp privacy.html dist/ && cp imprint.html dist/ && cp -r dist/assets dist/ 2>/dev/null || true",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "functions": {
    "api/*.js": {
      "maxDuration": 10
    }
  }
}
